# fruits = ["apple", "mango", "kiwi", "grapes"]
# # for fruit in fruits:
# #     print(f"I like to eat {fruit} everyday.")
#
# for index in range(0, len(fruits)):
#     print(f"{index+1} : {fruits[index]}")

# Taking the input from the user and converting it into a string
# input = input("Enter the input to convert into list(space separated): ")
input = input("Enter the input to convert into list(',' separated): ")
print(input.split('t'))
